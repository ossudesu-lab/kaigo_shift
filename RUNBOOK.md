# シフト作成 運用手順

手を動かすための覚書。**なぜそうなっているか**は `shift_solver_design.md` を見る。

---

## 0. 最初の一回だけ

会社のPCに Python と3つのライブラリを入れる。

```
pip install ortools openpyxl pyyaml
```

動作確認した版: Python 3.14 / ortools 9.15 / openpyxl 3.1 / PyYAML 6.0

### Windows で文字化けするとき

PowerShell の既定が cp932 なので、日本語や記号でエラーになることがある。
実行前に一度これを打っておく。

```
$env:PYTHONIOENCODING = "utf-8"
```

毎回打つのが面倒なら、環境変数に登録しておく。

---

## 1. 毎月の作業

11月を作る例。**上から順に実行する。**

> **通しで確認済み**（2026年11月で手順1〜6を実行）。
> 白紙シートの取り込み、レイアウト違いの検出、`PM休` の読み飛ばしまで動作を確認した。

### 手順1　月別の設定を作る

`data/month_2026-11.yaml` を作り、その月の祝日を書く。

```yaml
holidays:
  - 2026-11-03   # 文化の日
  - 2026-11-23   # 勤労感謝の日

# 管理者がリーダーに入れない月だけ 0 を書く。ふだんは null のままでよい。
manager_leader_max: null
```

前月のファイルをコピーして日付を直すのが早い。

### 手順2　希望を書いてもらう用の白紙シートを出す

```
python src/write_excel.py 2026-11 --template="<帳票のパス>" --sheet=10月 --blank --out="out/11月_希望記入用.xlsx"
```

- `--sheet` には **対象月と同じ日数のシート**を指定する（11月は30日なので30日ぶんの列があるシート）
- 日付・曜日・氏名が入った、勤務欄だけ空のシートが出る

### 手順3　希望を書いてもらう

| 希望 | 書き方 |
|---|---|
| 休みたい | `休` と書いて**薄茶色**に塗る |
| 夜勤に入りたい | `入` と書いて薄茶色（明・休は自動で続くので塗らなくてよい） |
| 早出・遅出 | `Ｂ` `Ｃ` と書いて薄茶色 |
| **有給** | `有` と書く（**色は不要**） |

塗っていない箇所は「指定なし」としてソルバーが決める。
**プール外（管理者・相談員・看護・角田さん・橘さん・ニシャさん・二宮さん）の希望も読む。**

### 手順4　希望を取り込む

```
python src/read_wishes.py 2026-11 --template="out/11月_希望記入用.xlsx" --sheet=11月 --force
```

出てきた件数を確認する。過去の実績は月30〜85件だった。極端に少なければ
シート名かレイアウトが違っている可能性が高い。

`PM休` `13時` のような但し書きは**警告を出して読み飛ばす**（処理は止まらない）。
希望として扱われないので、その日を指定したいなら `休` などに直しておく。

### 手順5　解く

```
python src/shift_solver.py 2026-11
```

- 数秒〜数分で終わる
- **同じ入力なら前回の結果をそのまま返す**（待ち時間なし）
- 気に入らなければ `--again` を付けて引き直す。別の組み方が出る

### 手順6　Excelに書き戻す

```
python src/write_excel.py 2026-11 --template="<帳票のパス>" --sheet=10月 --from-csv=out/shift_2026-11_codes.csv
```

`out/シフト_2026-11.xlsx` が出る。**元の帳票には書き込まない。**

### 手順7　残りを手で埋める

自動で埋まるのは25名中17名。次の8行は空欄なので人が埋める。

- リハ2名（西谷さん・三上さん）
- 栄養士・送迎4名（小川さん・大賀さん・藤村さん・永井さん）
- 合計行2つ

### 手順8　採用が決まったら翌月の繰越を作る

```
python src/make_carryover.py actual_2026-11.csv
```

**「実際に採用したシフト」を渡すこと。** 手修正したなら、その最終版から作る。
渡したCSVが翌月の前提になるので、採用していない案を渡すと以降が全部ずれる。

---

## 2. 手で直したあとの確認

Excel で直したら、CSVに貼って検証にかけられる。

```
python src/validate_shift.py out/shift_2026-11_codes.csv
```

ハード制約（夜勤明けの翌日は休、連勤5日以内、遅出の翌日に早出を入れない等）を
15項目チェックする。`判定: OK` なら問題なし。

---

## 3. 職員が増えたり減ったりしたら

**2か所直す。** 片方だけだと黙って壊れる。

### `data/staff.csv`

1行足す（または消す）。主な列:

| 列 | 意味 |
|---|---|
| `in_pool` | 変則プール（早出・遅出・夜勤をする）なら true |
| `can_lead` | 当日リーダーを担えるか |
| `allowed_shifts` | 取れる勤務。日勤だけの人は `DAY\|OFF` |
| `fixed_off_weekdays` | 固定休の曜日（`土\|日` など） |
| `off_on_holidays` | 祝日も休むなら true |
| `monthly_days` | 月の出勤日数（例 14）。ここが入っていれば休日数は月の日数から自動計算 |
| `weekly_days` | 週の勤務日数（例 5） |
| `max_consecutive` | 連続勤務の上限 |

### `data/sheet_layout.yaml`

**帳票の何行目がその人か**を書く。行を挿入すると以下がずれるので、
そのときは全体を見直すこと（2026年9月に二宮さんの行が増えたときが実例）。

- `pool_rows` … 変則プール
- `daystaff_rows` … 日勤のみ職員
- `other_rows` … ソルバーが決めない行（書き戻し時に空欄化される）

帳票の並びが変わったら `layouts:` に新しい名前で足し、`default_layout` を差し替える。
古い月を読むときは `--layout` で昔の並びを指定する。

---

## 4. 詰まったときの見方

| メッセージ | 意味・対処 |
|---|---|
| `職員行として指定した行が、勤務表の行に見えません` | `--layout` が帳票の並びと違う。集計行を職員行と取り違えている |
| `シートの日数が合いません（シートN日 / 対象月M日）` | `--sheet` が対象月と違う日数のシートを指している |
| `未知の記号 'PM休' を飛ばしました` | 想定外の記号。**警告だけで処理は続く。** 希望として扱われないので、必要なら手で直す |
| `シートに希望が1件も書かれていません` | 白紙のまま。希望を書いてから取り込む |
| `集計行の値が N件` | `--layout` 違い。職員行の位置に COUNTIF の数式が見えている |
| `既にあります。上書きするなら --force` | 出力先が存在する。上書きしてよければ `--force` |
| `前回と同じ入力なので、保存済みの結果を使います` | 正常。引き直したいなら `--again` |
| `橙で塗られていますが記号が空です` | 色だけ塗って記号を書き忘れている |
| `解が見つかりませんでした（INFEASIBLE）` | 希望が厳しすぎるなど。固定希望を減らすか、休日数の設定を見直す |

### 結果を見るときに目を通すところ

サマリの「検証基準との突き合わせ」で、次が揃っていれば通常どおり。

```
未配置        : 早0 遅0 入0
リーダー不在   : 0日
希望未達      : 0件
休日数        : 目標未達が0名
連続勤務5日    : 0名
```

---

## 5. ファイルの役割

### 毎月さわる

| ファイル | 中身 |
|---|---|
| `data/month_YYYY-MM.yaml` | 祝日、管理者リーダーの上限 |
| `requests_YYYY-MM.csv` | 希望（手順4で自動生成） |
| `carryover_YYYY-MM.yaml` | 前月からの繰越（手順8で自動生成） |

### 変わったときだけさわる

| ファイル | 中身 |
|---|---|
| `data/staff.csv` | 職員マスタ |
| `data/sheet_layout.yaml` | 帳票の行番号 |
| `data/month.yaml` | 全月共通の設定（必要枠数・重み） |

### さわらない

`src/` の7ファイル。`out/` は出力先で、消しても作り直される。

---

## 6. 注意

- **実名は `src/` にも `data/` にも書かない。** 対応は帳票の並び順で持ち、
  `sheet_layout.yaml` には行番号だけを書く（設計メモ 0章）
- **元の帳票には絶対に書き込まない。** 書き戻しは必ずコピーを作ってから行う
- **繰越は採用した表から作る。** ここを間違えると翌月以降が全部ずれる
